services:
  modabot:
    build: .
    ports:
      - "5000:5000"
    environment:
      PORT: "5000"
      OLLAMA_HOST: "http://host.docker.internal:11434"
    volumes:
      - ./produtos/db:/app/produtos/db
    extra_hosts:
      - "host.docker.internal:host-gateway"
